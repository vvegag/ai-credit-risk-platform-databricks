# Bibliotecas Python usadas pelos notebooks (instaladas via %pip install nas próprias células).
# PySpark, Delta Lake e MLflow tracking já vêm no Databricks Runtime ML — não pinados aqui.
xgboost==2.0.3
scikit-learn==1.3.2
optuna==3.6.1
prophet
shap==0.44.0
# Pins abaixo reconciliados com 10_rag_agent/requirements.txt (mesmos pacotes, mesma versão
# mínima) pra evitar divergência entre os dois arquivos.
sentence-transformers>=2.2.0
faiss-cpu
scipy
seaborn==0.13.0
reportlab
pillow
faker
pypdf>=3.0.0
langchain>=0.1.0
langchain-community
langchain-databricks>=0.1.0
databricks-vectorsearch>=0.22.0
